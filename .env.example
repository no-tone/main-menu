# This value is embedded into client JS (PUBLIC_*), so it is not secret.
# Use a tailnet-only HTTPS URL for client-side vpn/down detection.
PUBLIC_TAILNET_PROBE_URL=https://your-device.your-tailnet.ts.net/health
# You can provide two URLs: `desktopUrl,mobileUrl` (mobile optional).
