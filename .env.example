# Optional Worker-only secrets. Do not expose these as PUBLIC_*.
# Create an OAuth client with `devices:core:read` scope.
TAILSCALE_OAUTH_CLIENT_ID=
TAILSCALE_OAUTH_CLIENT_SECRET=
TAILSCALE_OAUTH_SCOPE=devices:core:read
TAILSCALE_TAILNET=<your-tailnet>.ts.net
TAILSCALE_STATUS_DEVICE=<device-name>
